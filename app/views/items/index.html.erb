

<div class="contents">
  <h2>Pick Up</h2>
  <div class="items">
    <% @item.each do |item| %>
      <div class="item">
        <div class="item-image">
          <%= link_to image_tag(item.image, size: '250x250'), item_path(item.id) %>
          <p><%= item.name %></p>
          <p><%= item.price %>円(税込)</p>
        </div>
        <div class="item-info">
          <div class="bubble03">
           <%= item.description %> 
          </div>
          <div class="producer">
            <%= link_to image_tag(item.producer.image, size: '150x150'), producer_path(item.producer.id) %>
            <p><%= "#{item.producer.family_name}"+ "#{item.producer.first_name}" %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <div class="push"></div>
</div>

